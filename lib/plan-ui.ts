/**
 * Neutral slot for Pro-gated regions while `useUserPlan().loading` is true.
 * Use instead of rendering a premium widget or a locked teaser to avoid flicker.
 */
export const PRO_GATING_PLACEHOLDER_CLASS =
  "min-h-[10rem] rounded-2xl border border-white/5 bg-slate-950/25";
