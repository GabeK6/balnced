/** Primary disclaimer — use on dashboard shell, marketing footer, and key financial surfaces. */
export const TRUST_DISCLAIMER =
  "Estimates only — not financial advice.";

/** Secondary line — reinforces that outputs depend on user input (optional, very subtle). */
export const TRUST_DATA_NOTE = "Figures reflect what you enter.";

/** Copilot / AI-specific add-on (keep on the same line or directly below the primary disclaimer). */
export const TRUST_AI_LIMITATION = "AI can be mistaken — verify important decisions.";
